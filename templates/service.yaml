apiVersion: v1
kind: Service
metadata:
  name: {{ include "mkdocs-helm-chart.fullname" . }}
  labels:
    {{- include "mkdocs-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "mkdocs-helm-chart.selectorLabels" . | nindent 4 }}
